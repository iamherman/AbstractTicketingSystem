AbstractTicketingSystem
=======================

This is a Perl library to an Abstract Ticketing System (ZenDesk, RT, etc). It can be used as the layer between an IVR or whatever else needs to Interface with a ticketing system.
